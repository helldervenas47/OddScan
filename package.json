{
  "name": "oddscan-monorepo",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=@oddscan/web",
    "worker:dev": "npm run dev --workspace=@oddscan/worker",
    "worker:run": "npm run run:once --workspace=@oddscan/worker",
    "build": "npm run build --workspace=@oddscan/shared && npm run build --workspace=@oddscan/web",
    "test": "npm run test --workspaces --if-present"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
